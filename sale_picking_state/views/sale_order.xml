<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<record id="view_order_tree" model="ir.ui.view">
		<field name="model">sale.order</field>
		<field name="inherit_id" ref="sale.view_order_tree" />
		<field name="arch" type="xml">
			<field name="invoice_status" position="after">
				<field name="picking_state" />
			</field>
		</field>
	</record>

	<record id="view_order_form" model="ir.ui.view">
		<field name="model">sale.order</field>
		<field name="inherit_id" ref="sale.view_order_form" />
		<field name="arch" type="xml">
			<field name="picking_policy" position="after">
				<field name="picking_state" />
			</field>
		</field>
	</record>

	<record id="view_sales_order_filter" model="ir.ui.view">
		<field name="model">sale.order</field>
		<field name="inherit_id" ref="sale.view_sales_order_filter" />
		<field name="arch" type="xml">
			<filter name="my_sale_orders_filter" position="after">
				<separator />
				<filter string="Nothing Delivered" name="filter_picking_no"
					domain="[('picking_state', '=', 'not_delivered')]" />
				<filter string="Partial Delivered"
					name="filter_picking_partial"
					domain="[('picking_state', '=', 'partially_delivered')]" />
				<filter string="Fully Delivered"
					name="filter_picking_completed"
					domain="[('picking_state', '=', 'done')]" />
			</filter>
			<filter name="order_month" position="after">
				<separator />
				<filter name="group_by_picking_state" string="Picking Status"
					context="{'group_by': 'picking_state'}" />
			</filter>
		</field>
	</record>

</odoo>
