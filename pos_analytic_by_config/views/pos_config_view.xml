<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<!-- Inherit Form View to Modify it -->
	<record id="pos_config_form_view" model="ir.ui.view">
		<field name="name">pos.config.form.inherit</field>
		<field name="model">pos.config</field>
		<field name="inherit_id" ref="point_of_sale.pos_config_view_form" />
		<field name="arch" type="xml">
			<xpath expr="//field[@name='journal_id']/../../../.." position="after">
				<div class="col-12 col-md-6 o_setting_box" groups="analytic.group_analytic_accounting">
					<div class="o_setting_right_pane">
						<span class="o_form_label">Analytic Account</span>
						<div class="text-muted">
							Configuration Analytic Account
						</div>
						<div class="content-group">
							<div class="row mt16"
								title="Whenever you close a session, one entry is generated in the following accounting journal for all the orders not invoiced. Invoices are recorded in accounting separately.">
								<label for="account_analytic_id" class="col-md-3 o_light_label" />
								<field name="account_analytic_id" />
							</div>
						</div>
					</div>
				</div>
			</xpath>
		</field>
	</record>

</odoo>