<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_picking_type_form" model="ir.ui.view">
        <field name="name">stock.picking.type.form</field>
        <field name="model">stock.picking.type</field>
        <field name="inherit_id" ref="stock.view_picking_type_form" />
        <field name="arch" type="xml">
            <field name="show_operations" position="after">
                <field name="is_requisition" attrs="{'invisible': [('code','!=','outgoing')]}"/>
            </field>

            <field name="return_picking_type_id" position="attributes">
                <attribute name="attrs">{'invisible': [('code', 'not in', ['incoming', 'outgoing', 'internal'])], 'required': [('is_requisition','=',True)]}</attribute>
            </field>
        </field>
    </record>
</odoo>