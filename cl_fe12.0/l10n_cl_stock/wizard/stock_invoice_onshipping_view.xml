<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<record id="view_stock_invoice_onshipping" model="ir.ui.view">
		<field name="name">Stock Invoice Onshipping</field>
		<field name="model">stock.invoice.onshipping</field>
		<field name="inherit_id"
			ref="stock_picking_invoicing.view_stock_invoice_onshipping" />
		<field name="arch" type="xml">
			<field name="journal_id" position="after">
				<field name="journal_document_class_id"
					options="{'no_create': True}"
					attrs="{'required': [('use_documents', '=', True)], 'invisible': [('use_documents', '=', False)]}"
					domain="[('journal_id', '=', journal_id)]" />
				<field name="use_documents" invisible="1" />
			</field>
		</field>
	</record>
</odoo>
